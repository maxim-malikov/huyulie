# 🎧 Решение проблемы с наушниками

## Проблема
Когда звук идёт через наушники, микрофон MacBook не может его физически услышать. PyAudio захватывает только входные устройства (микрофоны), а не системный звук.

## Решение: BlackHole + Multi-Output Device

### Шаг 1: Установка BlackHole
```bash
brew install --cask blackhole-2ch
```

### Шаг 2: Настройка Multi-Output Device

1. Откройте **Audio MIDI Setup** (⌘+Space → "Audio MIDI Setup")

2. Нажмите **"+"** внизу слева → **"Create Multi-Output Device"**

3. Отметьте галочками:
   - ✅ **BlackHole 2ch**
   - ✅ **Ваши наушники** (AirPods, внешние наушники и т.д.)

4. Переименуйте устройство в **"Interview Audio"** (правый клик → Rename)

5. **ВАЖНО**: Убедитесь, что "Drift Correction" включен для BlackHole

### Шаг 3: Переключение звука

В **System Settings → Sound → Output** выберите **"Interview Audio"**

Теперь звук идёт одновременно:
- В ваши наушники (вы слышите)
- В BlackHole (можно захватывать программно)

### Шаг 4: Настройка захвата в скрипте

```bash
# Проверьте доступные устройства
cd ~/mlxwhisper
source .venv/bin/activate
python debug_audio.py --list-only

# Найдите номер BlackHole и протестируйте
python debug_audio.py --device [НОМЕР_BLACKHOLE] --duration 10

# Используйте улучшенный скрипт
cp rt_toggle_enhanced.py rt_toggle.py

# Или настройте через переменную окружения
export WHISPER_MIC="BlackHole 2ch"
```

### Шаг 5: Тестирование

1. Включите любое видео/аудио (YouTube, Zoom и т.д.)
2. Убедитесь, что звук идёт в "Interview Audio"
3. Запустите тест:
   ```bash
   python rt_toggle_enhanced.py --check-setup
   ```
4. Нажмите Ctrl+Option+W для записи

## Альтернативное решение: Loopback

Если не хотите использовать BlackHole, можно купить [Loopback](https://rogueamoeba.com/loopback/) ($99) - более удобный GUI.

## Быстрая диагностика

```bash
# Запустите диагностический скрипт
python debug_audio.py

# Если видите низкую амплитуду при воспроизведении звука через наушники:
# → BlackHole не настроен или не выбран Multi-Output Device
```

## Переключение режимов

**Для обычной работы:**
System Settings → Sound → Output → Ваши наушники

**Для интервью:**
System Settings → Sound → Output → Interview Audio

## Автоматизация (опционально)

Создайте shortcuts для быстрого переключения:

```bash
# Переключить на Interview Audio
osascript -e 'tell application "System Events" to set volume output device to "Interview Audio"'

# Вернуть обычные наушники
osascript -e 'tell application "System Events" to set volume output device to "AirPods Pro"'
```
# 📋 Инструкция по настройке MLX Whisper с поддержкой наушников

## Статус установки

✅ **Выполнено:**
- Создан бэкап конфигурации в `/Users/maxim_malikov/mlxwhisper/backups/`
- Обновлены скрипты с поддержкой BlackHole
- Добавлена автоматическая детекция BlackHole
- Создан диагностический скрипт `debug_audio.py`

⚠️ **Требует ручной установки:**
- BlackHole (требует sudo)

## Что нужно сделать

### 1. Установить BlackHole

Откройте Terminal и выполните:

```bash
brew install --cask blackhole-2ch
```

Введите пароль администратора когда попросят.

**Альтернативный вариант:**
1. Скачайте BlackHole с https://existential.audio/blackhole/
2. Запустите установщик .pkg

### 2. Настроить Multi-Output Device

1. Откройте **Audio MIDI Setup** (нажмите ⌘+Space и введите "Audio MIDI Setup")

2. Нажмите **"+"** в левом нижнем углу → **"Create Multi-Output Device"**

3. В правой панели отметьте галочками:
   - ✅ **BlackHole 2ch**
   - ✅ **Ваши наушники** (PLT Focus, AirPods или другие)

4. Важно: Включите **"Drift Correction"** для BlackHole 2ch

5. Переименуйте устройство:
   - Правый клик на "Multi-Output Device" → Rename
   - Назовите "Interview Audio"

### 3. Переключение режимов

**Для интервью (с записью звука из наушников):**
```bash
# System Settings → Sound → Output → Interview Audio
```

**Для обычной работы:**
```bash
# System Settings → Sound → Output → Ваши наушники (PLT Focus)
```

### 4. Тестирование

```bash
cd ~/mlxwhisper
source .venv/bin/activate

# Проверить устройства (BlackHole должен появиться)
python debug_audio.py --list-only

# Тест записи с BlackHole (найдите его номер в списке)
python debug_audio.py --device [НОМЕР_BLACKHOLE]

# Проверка автоматического определения
python rt_toggle.py --check-setup
```

### 5. Использование

После настройки Multi-Output Device:

1. Звук будет идти одновременно в наушники И в BlackHole
2. Скрипт автоматически обнаружит BlackHole и будет его использовать
3. Нажимайте Ctrl+Option+W как обычно для записи

## Текущая конфигурация

**Обнаруженные устройства:**
- MacBook Pro Microphone (встроенный)
- PLT Focus (ваши наушники, DEFAULT)
- iPhone Microphone
- Microsoft Teams Audio

**BlackHole:** ❌ Не установлен

## Восстановление оригинальной конфигурации

Если что-то пошло не так:

```bash
cd ~/mlxwhisper
# Восстановить оригинальные файлы
cp rt_toggle_original.py rt_toggle.py
tar -xzf backups/mlxwhisper_backup_*.tar.gz
```

## Проблемы и решения

**BlackHole не появляется после установки:**
- Требуется перезагрузка Mac после установки BlackHole

**Не захватывается звук:**
- Убедитесь, что выбран "Interview Audio" в Sound Output
- Проверьте, что BlackHole отмечен в Multi-Output Device
- Убедитесь, что Drift Correction включен

**Слишком тихий звук:**
- Проверьте громкость в приложении (Zoom/Meet)
- Увеличьте системную громкость